# Minimal install — video upload + transcription only (~1.5 GB image vs ~6 GB full)
# Used by Dockerfile by default. For the full voice assistant, use requirements.txt.

fastapi==0.135.3
uvicorn[standard]==0.44.0
websockets==16.0
numpy>=1.24.0
httpx>=0.27.0
jinja2>=3.1.0
python-dotenv>=1.0.0
noisereduce==3.0.3

# Transcription
torch>=2.0.0
faster-whisper>=1.0.0
