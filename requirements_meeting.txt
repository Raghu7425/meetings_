# ── Teams Meeting Assistant — Additional Requirements ─────────────────────────
# Install alongside existing requirements.txt:
#   pip install -r requirements.txt -r requirements_meeting.txt
#
# After install, download models:
#   python -m spacy download en_core_web_sm
#   python -c "import nltk; nltk.download('vader_lexicon'); nltk.download('stopwords')"

# ── NLP pre-processing (no-LLM extraction) ─────────────────────────────────────
spacy>=3.7.0             # NER: PERSON, ORG, GPE, DATE — en_core_web_sm model
nltk>=3.8.0              # VADER sentiment + stopwords
rake-nltk>=1.0.6         # RAKE keyword extraction (pure algorithm, no model)
dateparser>=1.2.0        # Relative date parsing: "next Friday", "end of month"

# Transcription with speaker diarization
whisperx>=3.1.0          # recommended — wraps faster-whisper + alignment + diarization
# faster-whisper>=1.0.0  # already in requirements.txt (fallback if whisperx not available)

# Optional: pyannote for diarization (requires HF token)
# pyannote.audio>=3.0.0  # uncomment if you want speaker labels via HuggingFace

# Database (async PostgreSQL)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0

# Scheduling
apscheduler>=3.10.0

# Object storage (MinIO/S3)
boto3>=1.34.0

# Email templating
jinja2>=3.1.0

# HTTP client (async)
httpx>=0.27.0

# Microsoft Graph API auth
# (uses httpx — no extra SDK needed; msal can be added for advanced scenarios)
# msal>=1.28.0

# Redis (async client — redis.asyncio)
redis>=5.0.0
async-timeout>=4.0.0

# Qdrant vector database client
qdrant-client>=1.9.0

# Retry logic
tenacity>=8.2.0

# Scientific computing (cosine similarity)
scipy>=1.10.0

# Sync HTTP (Ollama agent calls)
requests>=2.31.0

# Misc
python-multipart>=0.0.9
python-dotenv>=1.0.0
